#include "binary_trees.h"

binary_tree_t *binary_tree_node(binary_tree_t *parent, int value)
{
    binary_tree_t = malloc(sizeof(binary_tree_t));
    if (binary_tree_t == NULL)
        return(NULL);
    
    binary_tree_t parent = Node(value);
    return(Node);
}